#include <iostream>
#include <iomanip>
#define SIZE 10

using namespace std;

int main(){
int n,i;
int *p;
cout<<“How many numbers needed?”;
cin>> n;
p=new int[n];
for(i=0;i<n;i++) {
        cout<<"Input value"<<i<<":";
        cin>>*(p+i);
        p++;
}

// Reverse
int temp, *pb =p+n-1; //?
cout<<"Reverse: ";
for (i=0; i < n / 2; i++){
    temp = *p;
    *p = *pb;
    *pb = temp;
    p++; pb--;
}
p-=(n/2);

for(i=0;i<n;i++) {
        cout<<*p<<endl;p++;
}
p-=n;
delete [] p;
return 0;
}
